import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'test.example.app',
  appName: 'test1',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
